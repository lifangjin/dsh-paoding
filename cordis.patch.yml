# dsh-paoding bundle patch — `dsh plugin --profile <name> add dsh-paoding` 时由
# reconcilePlugins 并入该 profile 的 dsh.profile.bundles，boot 时并入补丁树。
# name: dsh-paoding 指向 pnpm 装进 profile 的本包（main: plugins/paoding-config-ui/index.mjs）。
- insert:
    - id: paoding-config-ui
      name: dsh-paoding
